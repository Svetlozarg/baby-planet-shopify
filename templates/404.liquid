<div class="baby-404-container">
  <div class="baby-404-content">
    <!-- Baby-themed illustration area -->
    <div class="baby-404-illustration">
      <div class="baby-404-number">
        <span class="number-4">4</span>
        <div class="baby-rattle">
          <div class="rattle-head"></div>
          <div class="rattle-handle"></div>
          <div class="rattle-beads">
            <div class="bead"></div>
            <div class="bead"></div>
            <div class="bead"></div>
          </div>
        </div>
        <span class="number-4">4</span>
      </div>

      <!-- Floating baby elements -->
      <div class="floating-elements">
        <div class="baby-cloud cloud-1"></div>
        <div class="baby-cloud cloud-2"></div>
        <div class="baby-star star-1">★</div>
        <div class="baby-star star-2">★</div>
        <div class="baby-star star-3">★</div>
        <div class="baby-balloon balloon-1"></div>
        <div class="baby-balloon balloon-2"></div>
      </div>
    </div>

    <!-- Content section -->
    <div class="baby-404-text">
            <h1 class="baby-404-title">404 - Страницата не е намерена</h1>
      <p class="baby-404-subtitle">Упс! Тази малка страничка се изгуби в детската стая</p>
      <div class="baby-404-description">
        <p>Страницата, която търсите, не съществува. Кликнете <a href="/collections/all">тук</a>, за да продължите с пазаруването.</p>
      </div>

      <!-- Action buttons -->
      <div class="baby-404-actions">
        <a href="{{ shop.url }}" class="baby-btn baby-btn-primary">
          <span>Към Начало</span>
          <i class="baby-heart">♥</i>
        </a>
        <a href="/collections/all" class="baby-btn baby-btn-secondary">
          <span>Бебешки Продукти</span>
          <i class="baby-toy">🧸</i>
        </a>
      </div>
    </div>
  </div>
</div>

<style>
.baby-404-container {
  min-height: 80vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 40px 20px;
  background: linear-gradient(135deg, #f8f9fa 0%, #fff5f5 100%);
  font-family: {{ settings.base_font | font_family }}, sans-serif;
  position: relative;
  overflow: hidden;
  border-radius: 10px;
}

.baby-404-content {
  text-align: center;
  max-width: 800px;
  margin: 0 auto;
  position: relative;
  z-index: 2;
}

/* Baby-themed 404 illustration */
.baby-404-illustration {
  position: relative;
  margin-bottom: 40px;
}

.baby-404-number {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 20px;
  margin-bottom: 20px;
}

.number-4 {
  font-size: 120px;
  font-weight: bold;
  color: {{ settings.color_primary | default: '#ef7b84' }};
  text-shadow: 0 4px 8px rgba(239, 123, 132, 0.3);
  animation: bounce 2s infinite;
}

.baby-rattle {
  position: relative;
  width: 60px;
  height: 100px;
  margin: 0 10px;
  animation: shake 1.5s infinite;
}

.rattle-head {
  width: 60px;
  height: 60px;
  background: {{ settings.primary_hover_btn | default: '#00d4c5' }};
  border-radius: 50%;
  position: relative;
  border: 4px solid {{ settings.color_primary | default: '#ef7b84' }};
  box-shadow: 0 4px 12px rgba(0, 212, 197, 0.3);
}

.rattle-handle {
  width: 8px;
  height: 40px;
  background: {{ settings.color_heading_color | default: '#685f52' }};
  margin: 0 auto;
  border-radius: 4px;
}

.rattle-beads {
  position: absolute;
  top: 10px;
  left: 50%;
  transform: translateX(-50%);
  display: flex;
  gap: 8px;
}

.bead {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: {{ settings.color_primary | default: '#ef7b84' }};
  animation: rattle-beads 1.5s infinite;
}

.bead:nth-child(2) {
  background: {{ settings.primary_hover_btn | default: '#00d4c5' }};
  animation-delay: 0.2s;
}

.bead:nth-child(3) {
  background: #ffd4ca;
  animation-delay: 0.4s;
}

/* Floating elements */
.floating-elements {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  pointer-events: none;
  z-index: 1;
}

.baby-cloud {
  position: absolute;
  width: 60px;
  height: 30px;
  background: rgba(255, 255, 255, 0.8);
  border-radius: 30px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
}

.baby-cloud:before {
  content: '';
  position: absolute;
  top: -15px;
  left: 10px;
  width: 25px;
  height: 25px;
  background: rgba(255, 255, 255, 0.8);
  border-radius: 50%;
}

.baby-cloud:after {
  content: '';
  position: absolute;
  top: -10px;
  right: 15px;
  width: 20px;
  height: 20px;
  background: rgba(255, 255, 255, 0.8);
  border-radius: 50%;
}

.cloud-1 {
  top: 20%;
  left: 10%;
  animation: float 6s ease-in-out infinite;
}

.cloud-2 {
  top: 30%;
  right: 10%;
  animation: float 8s ease-in-out infinite reverse;
}

.baby-star {
  position: absolute;
  color: #ffd4ca;
  font-size: 24px;
  animation: twinkle 2s infinite;
}

.star-1 {
  top: 15%;
  left: 20%;
  animation-delay: 0s;
}

.star-2 {
  top: 25%;
  right: 25%;
  animation-delay: 0.7s;
}

.star-3 {
  top: 40%;
  left: 75%;
  animation-delay: 1.4s;
}

.baby-balloon {
  position: absolute;
  width: 20px;
  height: 28px;
  border-radius: 50% 50% 50% 50% / 60% 60% 40% 40%;
  animation: float 5s ease-in-out infinite;
}

.balloon-1 {
  background: {{ settings.color_primary | default: '#ef7b84' }};
  top: 10%;
  right: 15%;
  animation-delay: 1s;
}

.balloon-2 {
  background: {{ settings.primary_hover_btn | default: '#00d4c5' }};
  top: 35%;
  left: 5%;
  animation-delay: 2s;
}

/* Text content */
.baby-404-text {
  margin-bottom: 60px;
}

.baby-404-title {
  font-size: 48px;
  color: {{ settings.color_heading_color | default: '#685f52' }};
  margin-bottom: 20px;
  font-weight: bold;
  line-height: 1.2;
}

.baby-404-subtitle {
  font-size: 24px;
  color: {{ settings.color_primary | default: '#ef7b84' }};
  margin-bottom: 20px;
  font-weight: 500;
}

.baby-404-description {
  font-size: 18px;
  color: {{ settings.color_body_text | default: '#685f52' }};
  margin-bottom: 40px;
  line-height: 1.6;
}

/* Action buttons */
.baby-404-actions {
  display: flex;
  gap: 20px;
  justify-content: center;
  margin-bottom: 40px;
  flex-wrap: wrap;
}

.baby-btn {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  padding: 16px 32px;
  border-radius: 25px;
  text-decoration: none;
  font-weight: 600;
  font-size: 16px;
  transition: all 0.3s ease;
  border: 2px solid transparent;
  position: relative;
  overflow: hidden;
}

.baby-btn-primary {
  background: {{ settings.color_primary | default: '#ef7b84' }};
  color: {{ settings.color_button_primary_text | default: '#ffffff' }};
  box-shadow: 0 4px 15px rgba(239, 123, 132, 0.3);
}

.baby-btn-primary:hover {
  background: {{ settings.primary_hover_btn | default: '#00d4c5' }};
  color: {{ settings.primary_hover_btn_text | default: '#fff' }};
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(0, 212, 197, 0.4);
}

.baby-btn-secondary {
  background: transparent;
  color: {{ settings.color_primary | default: '#ef7b84' }};
  border-color: {{ settings.color_primary | default: '#ef7b84' }};
}

.baby-btn-secondary:hover {
  background: {{ settings.color_primary | default: '#ef7b84' }};
  color: {{ settings.color_button_primary_text | default: '#ffffff' }};
  transform: translateY(-2px);
}

.baby-heart,
.baby-toy {
  font-size: 18px;
  animation: pulse 1.5s infinite;
}

/* Search section */
.baby-404-search {
  margin-bottom: 40px;
}

.search-label {
  font-size: 16px;
  color: {{ settings.color_body_text | default: '#685f52' }};
  margin-bottom: 15px;
}

.baby-search-form {
  display: flex;
  max-width: 400px;
  margin: 0 auto;
  position: relative;
  border-radius: 25px;
  overflow: hidden;
  box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
}

.baby-search-input {
  flex: 1;
  padding: 16px 20px;
  border: none;
  outline: none;
  font-size: 16px;
  background: white;
  color: {{ settings.color_body_text | default: '#685f52' }};
}

.baby-search-input::placeholder {
  color: #a0a0a0;
}

.baby-search-btn {
  padding: 16px 20px;
  background: {{ settings.color_primary | default: '#ef7b84' }};
  border: none;
  cursor: pointer;
  color: white;
  transition: background 0.3s ease;
}

.baby-search-btn:hover {
  background: {{ settings.primary_hover_btn | default: '#00d4c5' }};
}

/* Categories section */
.baby-404-categories {
  text-align: center;
  max-width: 600px;
  margin: 0 auto;
}

.baby-404-categories h3 {
  font-size: 24px;
  color: {{ settings.color_heading_color | default: '#685f52' }};
  margin-bottom: 30px;
  font-weight: 600;
}

.category-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
  gap: 20px;
}

.category-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 20px;
  background: white;
  border-radius: 15px;
  text-decoration: none;
  color: {{ settings.color_body_text | default: '#685f52' }};
  transition: all 0.3s ease;
  box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
  border: 2px solid transparent;
}

.category-item:hover {
  transform: translateY(-5px);
  box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
  border-color: {{ settings.color_primary | default: '#ef7b84' }};
}

.category-icon {
  font-size: 32px;
  margin-bottom: 10px;
  filter: grayscale(0.2);
  transition: filter 0.3s ease;
}

.category-item:hover .category-icon {
  filter: grayscale(0);
}

.category-item span {
  font-weight: 500;
  font-size: 14px;
}

/* Animations */
@keyframes bounce {
  0%, 20%, 50%, 80%, 100% {
    transform: translateY(0);
  }
  40% {
    transform: translateY(-10px);
  }
  60% {
    transform: translateY(-5px);
  }
}

@keyframes shake {
  0%, 100% { transform: rotate(0deg); }
  25% { transform: rotate(5deg); }
  75% { transform: rotate(-5deg); }
}

@keyframes rattle-beads {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.2); }
}

@keyframes float {
  0%, 100% { transform: translateY(0px); }
  50% { transform: translateY(-20px); }
}

@keyframes twinkle {
  0%, 100% { opacity: 0.3; transform: scale(1); }
  50% { opacity: 1; transform: scale(1.2); }
}

@keyframes pulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.1); }
}

/* Responsive design */
@media (max-width: 768px) {
  .baby-404-container {
    padding: 20px 15px;
  }

  .number-4 {
    font-size: 80px;
  }

  .baby-404-title {
    font-size: 36px;
  }

  .baby-404-subtitle {
    font-size: 20px;
  }

  .baby-404-description {
    font-size: 16px;
  }

  .baby-404-actions {
    flex-direction: column;
    align-items: center;
  }

  .baby-btn {
    padding: 14px 28px;
    font-size: 14px;
  }

  .category-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 15px;
  }

  .category-item {
    padding: 15px;
  }

  .category-icon {
    font-size: 28px;
  }

  /* Hide some floating elements on mobile */
  .star-2, .star-3, .balloon-2 {
    display: none;
  }
}

@media (max-width: 480px) {
  .baby-404-number {
    gap: 10px;
  }

  .number-4 {
    font-size: 60px;
  }

  .baby-rattle {
    width: 40px;
    height: 70px;
  }

  .rattle-head {
    width: 40px;
    height: 40px;
  }

  .baby-404-title {
    font-size: 28px;
  }

  .baby-404-subtitle {
    font-size: 18px;
  }
}

/* Add some background decoration */
.baby-404-container::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-image:
    radial-gradient(circle at 20% 80%, rgba(255, 212, 202, 0.2) 0%, transparent 50%),
    radial-gradient(circle at 80% 20%, rgba(100, 220, 246, 0.2) 0%, transparent 50%),
    radial-gradient(circle at 40% 40%, rgba(239, 123, 132, 0.1) 0%, transparent 50%);
  z-index: 1;
}
</style>
